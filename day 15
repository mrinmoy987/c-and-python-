#include <stdio.h>
int main() {
    int num, count = 0;
    printf("Enter a number: ");
    scanf("%d", &num);
    while (num != 0) {
        num /= 10;
        count++;
    }
    printf("Digits: %d", count);
    return 0;
}

num = input("Enter a number: ")
print("Digits:", len(num))
